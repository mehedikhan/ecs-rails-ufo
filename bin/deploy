#!/bin/bash

# Generated starter bin/deploy script example.
# Meant to be overridden.

# Only build one docker image and deploys it to multiple ECS services
ufo ships ecs-rails-ufo-{web,clock,worker}
